<?xml version="1.0" encoding="UTF-8"?>
<suppressions xmlns="https://jeremylong.github.io/DependencyCheck/dependency-suppression.1.3.xsd">

    <!--
        Suppression file for OWASP dependency-check.

        Each <suppress> block must include a notes element explaining WHY this CVE
        is suppressed for this project. Acceptable reasons:
          - False positive (dependency-check matched the wrong artifact).
          - Vulnerability only exposed in a code path Mojentic does not exercise.
          - Already fixed upstream but the CVE database has not caught up.

        Do NOT suppress CVEs you have not investigated. Better to leave a build
        failing than to bury a real vulnerability.

        Re-evaluate every suppression on each minor release. Stale suppressions
        with no "until: <YYYY-MM-DD>" review date should be removed.

        Format reference:
        https://jeremylong.github.io/DependencyCheck/general/suppression.html
    -->

    <!-- Currently empty — no known false positives or accepted findings.
         Add entries below as they arise. -->

</suppressions>
